server:
  port: 80
spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://mydbinstance2.cmcjn8qqcxqk.ap-northeast-1.rds.amazonaws.com:3306/connectU?serverTimezone=UTC
      username: admin
      password: GBb4ddyE1mfrBFGQA5sc

  # ========================================================================
  #Swagger配置
  # 启动报错需要修改以下mvc配置 Failed to start bean 'documentationPluginsBootstrapper'
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  # ========================================================================
  #mybatis plus
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
  # ========================================================================

springfox:
  documentation:
    swagger-ui:
      enabled: true
  #=============================================================
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  ##==============================================================
  #熱部屬配置
  devtools:
    restart:
      # 设置开启热部署
      enabled: true
      # 重启目录
      additional-paths: src/main/java
      exclude: WEB-INF/**
  #页面不加载缓存，修改即时生效
  freemarker:
    cache: false

  # ========================================================================

  #    transaction:
  #      # 配置事务管理器
  #      platform: org.springframework.jdbc.datasource.DataSourceTransactionManager
  # ========================================================================
  #spring:
  #  datasource:
  #    druid:
  #      driver-class-name: com.mysql.cj.jdbc.Driver
  #      url: jdbc:mysql://localhost:3306/iii?serverTimezone=UTC
  #      username: root
  #      password: root
  # ========================================================================




